import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Verdant Dark)
  static const Color primary = Color(0xFFB0FF92); // Acid Neon Green (Accents)
  static const Color secondary = Color(0xFF00563A); // Deep Forest Green (Foundations)
  
  // Surface Colors
  static const Color obsidian = Color(0xFF0A0F0B); // Deepest Green-Black (Background)
  static const Color forestCard = Color(0xFF1B2E1D); // Dark Forest Green (Cards)
  static const Color glassBorder = Color(0xFF2C3E2D); // Subtle Green Border
  
  // Functional Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF94A3B8); // Slate Gray
  static const Color accent = Color(0xFFB0FF92);
  
  // Status Colors (Glow enabled)
  static const Color signalRed = Color(0xFFFF4B4B);
  static const Color signalYellow = Color(0xFFFFD43B);
  static const Color signalGreen = Color(0xFF22C55E);

  // Glassmorphic Overlays
  static Color glassBackground = Colors.white.withOpacity(0.05);
}
